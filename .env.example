# Host port exposed by docker compose for the API container (default 8000).
API_PORT=8000

# Optional work budgets (defaults shown; see README "安全阀" / sensitivity section).
# DECONVOLVER_MAX_SEARCH_OPS=20000000
# DECONVOLVER_MAX_SENSITIVITY_OPS=20000000
